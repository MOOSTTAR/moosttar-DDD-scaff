# Empty Javadoc JAR

This artifact is a **Maven Archetype** (`packaging: maven-archetype`): it contains no Java sources, only resource templates under `archetype-resources/`.

This empty `-javadoc.jar` is provided solely to satisfy the Maven Central publishing rule that every non-POM artifact must ship a `-javadoc.jar`. It carries no documentation.
